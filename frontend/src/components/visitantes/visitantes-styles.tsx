import styled from 'styled-components';

export const Form = styled.form`
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  margin-bottom: 24px;
`;

export const SaidaButton = styled.button`
  color: #fff;
  background: #d33;
  border: 0;
  border-radius: 4px;
  padding: 4px 12px;
  cursor: pointer;
  &:hover { background: #a00; }
`;

